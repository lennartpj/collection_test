galaxy_info:
  author: Lennart Pieperjohanns
  description: Ansible role to change root password and disable root login over SSH.
  license: license (MIT)
  min_ansible_version: 2.4
  platforms:
  - name: EL
    versions:
    - all

  galaxy_tags:
    - ssh
    - root
    - password
    - security
dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
